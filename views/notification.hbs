
<script type="application/javascript" src="/javascripts/worker_register.js"></script>
<h1>
알림 워커를 등록하는 중입니다...
</h1>

<svg style="width:100%;height:100px;background-color:#000000;">
  <circle id="circle" cx="50" cy="50" r="50"
          stroke="green" stroke-width="4" fill="yellow" ></circle>
  Sorry, your browser does not support inline SVG.
</svg>
<p id="aaa"></p>
<script>
  window.onload = ()=>{
      setTimeout(()=>{
          var circle = document.getElementById("circle");
          var inc = circle.parentNode.width.baseVal.value;
          setInterval(()=>{
              if(circle.parentNode.width.baseVal.value < Number(circle.getAttribute('cx'))+50
                      || 0>Number(circle.getAttribute('cx'))-50){
                  inc = -inc;
                  if(Math.abs(inc)>3)
                      inc *= 0.95;
                  circle.setAttribute('cx',Number(circle.getAttribute('cx'))+inc);
              }
              circle.setAttribute('cx',Number(circle.getAttribute('cx'))+inc);
              //document.getElementById('aaa').innerText = circle.getAttribute('cx');
          });
      },100);
  }
</script>
